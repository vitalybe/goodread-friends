var React = require('react');

module.exports = React.createClass({
    displayName: 'Login',
    render: function () {
        return (
            <div>
                This is the login page
            </div>
        )
    }
});